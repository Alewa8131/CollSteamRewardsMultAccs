# 🎮 CollSteamRewardsMultAccs

> Автоматизированный сбор бесплатных игр и предметов Steam Points Shop  
> **с нескольких аккаунтов одновременно**, без использования официального Steam API для покупок.

---

## 📌 Описание проекта

**CollSteamRewardsMultAccs** — это Python-скрипт для автоматического получения:

- 🆓 **бесплатных игр** из Steam Store  
- 🎁 **бесплатных предметов** из Steam Points Shop  

📦 **Сразу с нескольких аккаунтов**, используя:
- логин, пароль, `.maFile` (Steam Guard)
- асинхронную авторизацию через `aiosteampy`
- эмуляцию действий пользователя через **Playwright**

Проект специально разработан **без прямого использования `steam` / `steampy` для покупок**, чтобы:
- повторять реальное поведение браузера
- обходить ограничения Steam API
- корректно работать с модальными окнами, редиректами и проверкой возраста

---

## ✨ Основные возможности

### 🎮 Steam Store
- Поддержка всех типов страниц Steam:
  - **Add to Account**
  - **Add to Library**
  - **Install Game / Download**
- Автоматическая:
  - проверка владения игрой
  - обработка age-check страниц
  - закрытие модальных окон после добавления

### 🎁 Steam Points Shop
- Сбор **бесплатных предметов (0 points)**:
  - аватары
  - фоны профиля
  - рамки
  - стикеры
  - и прочее
- Кэширование protobuf’ов в `config.py`
- Повторный выкуп **без браузера**

---

## 🚀 Как запустить проект

Ниже описан **полный и обязательный порядок действий** для корректного запуска скрипта.  
Если пропустить любой из шагов — скрипт **не сможет авторизоваться или обработать аккаунты**.


### 1️⃣ Установка зависимостей

Убедитесь, что у вас установлен **Python 3.10 или выше**.

Установите зависимости:

```bash
pip install -r requirements.txt
playwright install
```

### 2️⃣ Настройка переменных окружения (.env)

В корне проекта создайте файл `.env` и укажите пароли от всех Steam-аккаунтов, которые будут использоваться.

Формат записи строго обязательный:

```bash
STEAM_PASS_name_account=yourPassword
STEAM_PASS_name_account1=yourPassword1
STEAM_PASS_name_account2=yourPassword2
```

- Где:
  - name_account должно совпадать с логином в `.env`
  - значение после "=" пароль от этого аккаунта
- Важно:
  - имя аккаунта должно полностью совпадать со значением `account_name` в соответствующем `.maFile`
  - никаких кавычек
  - никаких пробелов
  - каждый аккаунт указывается в отдельной строке

### 3️⃣ Добавление .maFile файлов

В папку `maFiles/` необходимо поместить `.maFile` для каждого аккаунта, используемого в `.env`

```bash
maFiles/
├── name_account.maFile
├── name_account1.maFile
└── name_account2.maFile
```
#### Пример структуры mafile:
```bash
{
  "shared_secret": "LeTT3RS6anDNuMBErs+OTh3RS/1=",
  "serial_number": "1234511187654321011",
  "revocation_code": "R05432",
  "uri": "otpauth://totp/Steam:name_account?secret=...",
  "account_name": "name_account",
  "identity_secret": "aBBa120x34Som/PleS3crE/T13a=",
  "Session": {
    "SteamID": 76561199876543210
  }
}
```

### 4️⃣ Настройка целей (urls.txt)
В файл `urls.txt` добавьте ссылки на игры или страницы магазина очков в `steam`. Скрипт зайдет на каждый аккаунт и соберет всё доступное по этим ссылкам.
 - Ссылки на бесплатные игры.
 - Ссылки на предметы за 0 очков в Steam Points Shop.
```bash
https://store.steampowered.com/app/583950
```

--- 
*После этих действий можно запускать `main.py` и наслаждаться сэкономленным временем!*
